import "Fib.ek"
import <io>

func printFib(int min, int max)
{
	for (int n = min; n < max; ++n)
	{
		print(fib(n));
	}
}

// NB: python ~112ms/~9MB --- Eucleia 2024-11-13: ~12s/~500MB (debug mode) : 2024-11-18: ~23s/~400kB (debug mode)
printFib(1, 25);
