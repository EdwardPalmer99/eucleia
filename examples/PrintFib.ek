func printFib(int min, int max)
{
	for (int n = min; n < max; ++n)
	{
		print(fib(n));
	};
};

func fib(int n)
{
	if (n > 2)
	{
		return fib(n - 1) + fib(n - 2);
	};
	if (n == 1 || n == 2)
	{
		return 1;
	};
	
	return 0;
};

printFib(1, 10);
