import "Fib.ek"
import <io>

func printFib(int min, int max)
{
	for (int n = min; n < max; ++n)
	{
		print(fib(n));
	}
}

// NB: python ~112ms/~9MB --- Eucleia 2024-11-07: ~22.5s/~500MB
printFib(1, 25);
