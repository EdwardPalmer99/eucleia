func printFib(min : int, max : int)
{
	for (n : int = min; n < max; n = n + 1)
	{
		print(fib(n));
	};
};

func fib(n : int)
{
	if (n > 2)
	{
		return fib(n - 1) + fib(n - 2);
	};
	if (n == 1 || n == 2)
	{
		return 1;
	};
	
	return 0;
};

printFib(1, 29);